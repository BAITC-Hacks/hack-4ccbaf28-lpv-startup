# LPV Match — план реализации

Текущее состояние: реализованы разговорный веб-интерфейс, импорт 66 анкет, фильтры, стабильное ранжирование, проверяемые альтернативы, AI-разбор текста и объяснения с проверкой цитат. Пройдены 22 теста и production-сборка. Команды запуска и демонстрация: [RUNBOOK.md](RUNBOOK.md).

Черновик заявки, календарь доступности и транскрипция подключены к интерфейсу. Подбор обновляется после подтверждения; избранное хранится локально в браузере. Текущий чат использует `/api/brief`, старый `/api/agent` оставлен для совместимости. Реальная транскрипция синтетической записи и мобильный интерфейс проверены командой; физический микрофон пользователя не записывался. Подробности — [VOICE-AND-BRIEF.md](VOICE-AND-BRIEF.md).

## Задача и пользовательский сценарий

Трек «Креативные индустрии», задача Firebird: подобрать до трёх подрядчиков для мероприятия из предоставленного организаторами каталога 66 анкет. Пользователь задаёт город, дату, категорию, формат мероприятия и бюджет; при необходимости — язык, продолжительность и пожелания к стилю.

Пример сценария: описать корпоратив текстом или голосом → проверить и подтвердить черновик → получить доступных подрядчиков с индивидуальными объяснениями → изменить дату и подтвердить новый подбор.

Техническое задание: https://docs.google.com/document/d/1rhR2HFY164BrnkIP39N3usY9fNY4JeAgkPxEzbqL00w/edit

## Архитектура MVP

- Next.js и TypeScript: интерфейс и серверные API в одном локально запускаемом приложении.
- Локальный каталог из официального CSV; отдельная база данных и GPU не требуются.
- Код проверяет город, категорию, дату занятости, бюджет, формат, язык и длительность. Модель не может отменить эти ограничения.
- Ранжирование детерминированное: одинаковые входные поля дают одинаковый порядок, при равных баллах используется идентификатор анкеты.
- LLM разбирает свободный запрос и помогает объяснить найденных кандидатов. Объяснения опираются только на факты из анкет и расчётов.
- Поиск возвращает три различных состояния: найдено; категории нет в городе; категория есть, но кандидаты не проходят ограничения.
- Не выполняем бронирование, отправку заявок и уведомления подрядчикам.

## Экономия API

Модель для простого извлечения полей и модель для более сложных объяснений настраиваются отдельно: `OPENAI_MODEL_FAST` и `OPENAI_MODEL_REASONING`. Текущая конфигурация — `gpt-5.4-nano` и `gpt-5.4-mini`; обе проверены реальными вызовами. Доступность на других аккаунтах зависит от их настроек.

Фильтрация, проверка занятости и сортировка не расходуют токены. Используются ограниченный контекст с тремя выбранными анкетами, ограничение длины ответов и кеш одинаковых запросов. Панель выполнения показывает использованную модель, задачу, фактические токены, время и оценочную стоимость. Без ключа работает явно обозначенный режим локального поиска с объяснениями по фактам.

Дообучение не требуется. Ключ хранится только в `.env.local`, обращения к провайдеру выполняются на сервере. NVIDIA подключаем лишь при реальной необходимости и подтверждённой совместимости API; наличие кредитов само по себе не требует второго провайдера.

## Реализованные этапы

1. Импорт и проверка 66 анкет; реализация строгих фильтров и стабильного ранжирования; тесты граничных случаев.
2. Форма, карточки, причины отсутствия результатов и демонстрационные запросы.
3. Разбор естественного языка, объяснения и маршрутизация моделей; измерение расхода API.
4. Проверка сборки, три демонстрационных сценария, инструкция независимого запуска и финальная подготовка.

Пушим завершённые содержательные этапы несколько раз в час. Перед отправкой проверяем изменения коллеги; не используем force push и пустые коммиты. README ведёт коллега, технические заметки — в `docs/`.

## Критерии готовности

- В выдаче не более трёх анкет; занятый на выбранную дату подрядчик исключён.
- Бюджет и остальные заданные ограничения соблюдены; цена обозначена как начальная.
- Повтор одного запроса сохраняет порядок результатов.
- Для каждой карточки есть индивидуальные объяснения с проверяемыми основаниями.
- Различаются отсутствие категории и отсутствие подходящих исполнителей.
- При одном или двух результатах показано действительное количество и причина сокращения.
- Проверены массовая категория, редкая категория и пустая выдача.
- Ошибка API не ломает локальный подбор; режим без LLM честно обозначен.
- Секреты не попадают в Git, браузерный код и диагностические ответы.
- README соответствует фактически проверенной команде запуска и текущим возможностям.


## Реализация AI

`src/server/brief-agent.ts`: nano вызывает `update_brief`, возвращает частичную заявку и уточнение; параметры валидируются Zod, неизвестные значения остаются незаполненными. Передаётся до четырёх предыдущих сообщений. Пользователь подтверждает поиск отдельным действием; затем `src/server/search-service.ts` отбирает кандидатов и проверяет выбранные моделью цитаты точным сравнением. Старый `src/server/agent.ts` с `search_contractors` и `ask_clarification` сохранён для совместимости и текущим интерфейсом не используется.

`src/components/use-voice-recorder.ts` и `src/server/transcribe.ts`: запись до 60 секунд, явная остановка или отмена, расшифровка без сохранения аудио и передача текста в черновик. Версия заявки защищает ручные изменения от устаревших ответов. `src/domain/favorites.ts` валидирует избранное из хранилища браузера. AI-иллюстрации описаны в [VISUALS.md](VISUALS.md); они не служат фактами об исполнителях.

Следующий приоритет команды — проверка Definition of Done, прежде всего индивидуальности объяснений с опорой на источники. Расширение интерфейса приостановлено ради оценки качества подбора.

`src/server/openai.ts`: Responses API, таймаут 6,5 с на вызов, максимум 1100 выходных токенов, `store: false`, ограниченный кеш и учёт `usage`. Сложность объяснения определяется наличием пожеланий длиннее 15 символов или сочетанием языка и длительности. При отказе модели результаты поиска сохраняются.

Документация и тарифы: https://developers.openai.com/api/docs/models/gpt-5.4-nano и https://developers.openai.com/api/docs/models/gpt-5.4-mini (проверены 23.09.2026).
